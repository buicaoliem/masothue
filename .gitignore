node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# secrets
.env
.env*.local

# local Claude Code config
.claude/

# local preview output (not committed)
/tmp/

# Search Console exports (may contain query data); the tooling reads them, they are never committed
/data/gsc/
